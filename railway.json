{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd registry && npm install && npm run prisma:generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd registry && npm run prisma:push && npm run prisma:seed && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
